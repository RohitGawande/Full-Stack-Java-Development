<!DOCTYPE hibernate-configuration PUBLIC 
    "-//Hibernate/Hibernate Configuration DTD 3.0//EN" 
    "http://hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>

        <!-- JDBC connection properties -->
        <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="hibernate.connection.url">jdbc:mysql://localhost:3306/rohit</property>
        <property name="hibernate.connection.username">RohitG</property>
        <property name="hibernate.connection.password">RohitG</property>

        <!-- Hibernate dialect for MySQL 8 -->
        <property name="hibernate.dialect">org.hibernate.dialect.MySQL8Dialect</property>

        <!-- Show SQL statements in the console -->
        <property name="hibernate.show_sql">true</property>

        <!-- Format SQL output -->
        <property name="hibernate.format_sql">true</property>

        <!-- Automatically create the schema -->
        <property name="hibernate.hbm2ddl.auto">create</property>

        <!-- Mapping class -->
        <mapping class="in.Rohit.model.Student"/>

    </session-factory>
</hibernate-configuration>
